# UI

| Folder | What it is |
|--------|------------|
| `app/` | Desktop + React application (recommended) |

Upstream master Streamlit + agent snapshot lives in repo-root `legacy/`.
Modern debugging logic lives in `sv_debug/`.
