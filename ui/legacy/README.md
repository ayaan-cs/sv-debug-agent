# Legacy UI

The full legacy Streamlit app + agent is preserved as one file at the repo root:

```powershell
streamlit run legacy.py
```

Or:

```powershell
.\start.ps1
```

Prefer `ui/app` for the desktop application.
